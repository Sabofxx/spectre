# macOS
.DS_Store

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/

# Generated site: deployed as a Pages artifact, never committed.
site/

# The database is NOT versioned: a ~96 MB binary rewritten on every run made
# the repository grow without bound (490 MB reached; GitHub's hard limit is
# 100 MB per file). It is carried by the `db-latest` release instead — the
# pipeline downloads it at the start of each run and re-publishes it at the end.
spectre.db
spectre.db-wal
spectre.db-shm

# Claude / assistant working files — never committed.
pour_claude.md
CLAUDE.md
.claude/
